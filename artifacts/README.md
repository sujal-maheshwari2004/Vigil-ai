# Artifacts

This directory holds generated MLOps artifacts such as versioned datasets, model files, and lightweight registry metadata.

Suggested layout:

- `datasets/` for captured training data snapshots
- `models/` for trained model artifacts
- `registry/` for latest-version pointers and promotion metadata
